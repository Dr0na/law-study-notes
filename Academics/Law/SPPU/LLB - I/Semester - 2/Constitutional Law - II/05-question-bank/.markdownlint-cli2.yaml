# Matches the rules this question-bank file historically opted into, with
# sensible defaults so lint passes without rewriting prose or headings.
config:
  MD013: false
  MD024:
    siblings_only: true
  MD026: false
  MD036: false
  # Collapsible recall grid uses <details>/<summary>.
  MD033: false
